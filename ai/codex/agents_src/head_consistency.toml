name = "pr_reviewer_consistency"
description = "Read-only PR reviewer for consistency with existing repository conventions."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = '''
