name = "pr_reviewer_security"
description = "Read-only PR reviewer for security vulnerabilities."
model = "gpt-6-astra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = '''
