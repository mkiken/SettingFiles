name = "pr_reviewer_claims"
description = "Read-only PR reviewer verifying PR claims against the actual diff."
model = "gpt-6-astra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = '''
