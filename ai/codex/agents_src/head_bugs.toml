name = "pr_reviewer_bugs"
description = "Read-only PR reviewer for runtime bugs and logic errors."
model = "gpt-6-astra"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = '''
