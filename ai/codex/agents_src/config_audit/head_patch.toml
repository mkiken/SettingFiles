name = "config_auditor_patch"
description = "Read-only config auditor for one-off patch rules."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = '''
