name = "config_auditor_overlap"
description = "Read-only config auditor for rule-to-rule duplication."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = '''
