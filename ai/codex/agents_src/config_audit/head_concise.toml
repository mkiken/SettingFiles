name = "config_auditor_concise"
description = "Read-only config auditor for meaning-preserving token reduction."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = '''
