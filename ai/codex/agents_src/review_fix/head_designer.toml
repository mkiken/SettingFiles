name = "review_fix_designer"
description = "Fix designer for review-fix: reads findings and code, writes a design file into the run directory."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "danger-full-access"

developer_instructions = '''
