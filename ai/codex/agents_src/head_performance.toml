name = "pr_reviewer_performance"
description = "Read-only PR reviewer for runtime performance regressions."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = '''
