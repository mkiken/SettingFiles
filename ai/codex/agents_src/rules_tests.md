Rules:
- Read both implementation and tests when judging coverage.
- Changed/new code is primary. Report missing or weak tests for unchanged code only when the untested path creates critical security breach, data corruption/loss, service outage, or compliance risk; prefix `[既存コード]` and name the category.
