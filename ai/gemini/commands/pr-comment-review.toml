description = "Analyzes PR review comments based on user instructions"
prompt = """Analyze PR review comments for {{args}}.

**Arguments**: treat the first `{{args}}` token as `COMMENT_URL`; the rest is `PROMPT`.

Fetch baseline PR comments with `run_shell_command`:
`gh pr view {pull_number} --comments`

Note: Dynamic `gh api` calls will require user confirmation during execution (Gemini CLI limitation).

### Core Analysis Rules

!{cat ~/.gemini/common/pr_comment_review_core.md}"""
