description = "Analyzes PR review comments based on user instructions"
prompt = """Analyze PR review comments for {{args}}.

**Arguments**: first token = PR comment URL ($COMMENT_URL); rest = user instructions ($PROMPT).

## Step 1: Parse the Comment URL

Extract comment type, ID, owner, repo, and pull_number from $COMMENT_URL (`https://github.com/{owner}/{repo}/pull/{pull_number}#...`):
- If URL contains `#issuecomment-{id}` → Issue Comment (comment on PR as a whole)
- If URL contains `#discussion_r{id}` → Review Comment (comment on code lines)

## Step 2: Fetch Baseline Data

After parsing, fetch all PR comments as baseline with `run_shell_command`:
`gh pr view {pull_number} --comments`

## Step 3: Fetch the Target Comment

Run the API matching the parsed URL:

- Issue Comment:
  ```bash
  gh api repos/{owner}/{repo}/issues/comments/{comment_id}
  ```
- Review Comment:
  ```bash
  gh api repos/{owner}/{repo}/pulls/comments/{comment_id}
  ```

Note: These dynamic API calls will require user confirmation during execution (Gemini CLI limitation).

## Step 4: Fetch Thread Context

For Review Comment, fetch all PR review comments and reconstruct the target thread with `in_reply_to_id`:

```bash
gh api repos/{owner}/{repo}/pulls/{pull_number}/comments
```

For Issue Comment, target is typically standalone.

## Step 5: Focused Analysis

Analyze the URL's **target comment** first; use related comments only as context.
Output detailed Japanese analysis in this structure:

### **Target Comment Details**

- **Author**:
- **Posted at**:
- **Location**: file path/line, if any
- **Content**:

### **Deep Analysis**

Based on $PROMPT, cover:

- Intent and purpose
- Pointed-out issues
- Technical background and reasoning
- Recommended response

### **Related Discussion**

- Same-thread discussion flow
- Relationship to the target comment

### **Recommended Actions**

- Specific actions
- Priority and direction

### **Additional Notes**

- Other important information"""
