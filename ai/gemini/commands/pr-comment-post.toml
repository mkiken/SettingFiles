description = "Post specific findings from pr-review results as GitHub PR inline comments"
prompt = """Post selected numbered findings from the previous `pr-review` result as one GitHub Pull Request Review for {{args}}.

## Instructions

- `ITEM_NUMBERS` = `{{args}}`.
- `{ai_header}` = `🤖 **Gemini Code Review**`.
- For every user confirmation, use the `ask_user` tool.

## Core Workflow

!{cat ~/.gemini/common/pr_comment_post_core.md}"""
